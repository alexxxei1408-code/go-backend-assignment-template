package main

// Job описывает единицу работы для пула воркеров.
type Job struct {
	ID    int
	Input int
}

// Result содержит результат обработки Job.
type Result struct {
	JobID  int
	Output int
}

// RunWorkerPool обрабатывает список jobs с помощью пула из workers воркеров.
//
// Требования:
//   - Каждый job должен быть обработан ровно один раз.
//   - Для вычисления результата нужно вызвать fn от поля Input.
//   - Возвращаемый слайс результатов должен содержать все результаты
//     и быть отсортирован по JobID по возрастанию.
//   - При workers <= 0 следует использовать один воркер.
//   - Для реализации рекомендуется использовать паттерн worker pool:
//     канал заданий, фиксированное число воркеров и канал результатов.
func RunWorkerPool(jobs []Job, workers int, fn func(int) int) []Result {
	// TODO: реализуйте worker pool
	return nil
}

// FanIn объединяет несколько входных каналов в один выходной.
//
// Требования:
//   - Функция должна запускать отдельные горутины для чтения из входных каналов.
//   - Выходной канал должен быть закрыт после закрытия всех входных.
//   - Порядок элементов в выходном канале не регламентируется.
func FanIn(chans ...<-chan int) <-chan int {
	// TODO: реализуйте fan-in на основе каналов и sync.WaitGroup
	return nil
}

func main() {}


